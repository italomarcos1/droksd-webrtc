"use client";

import React, { useState, useEffect, useCallback } from 'react';
// import { ITranscription } from '~/types';

// type Props = {
//   transcript: ITranscription;
//   setTranscript: ITranscription;
// }

// // TODO: migrar pro componente Room

export function SpeechRecognitionComponent() {
  

  

  // const handleStart = useCallback(() => {
  //   setListening(true);
  // }, []);

  // const handleStop = useCallback(() => {
  //   setListening(false);
  // }, []);

  return (
    <h1>oi</h1>
  );
};
